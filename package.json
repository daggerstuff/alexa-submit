{
  "name": "alexa-clinical-sim",
  "version": "0.3.0",
  "private": true,
  "license": "MIT",
  "description": "Clinical Conversation Coach for Alexa+ — scenario-based clinical communication simulator as a self-hosted MCP server",
  "scripts": {
    "start": "python -m server.mcp_server",
    "test": "pytest -q",
    "lint": "ruff check server tests && ruff format --check server tests"
  }
}
